import Link from "next/link";
import { Image } from "@/components/image";
import { SyncTabMdx } from "@/components/tab/sync-tab-mdx";

export const metadata = {
  title: "HTML",
};

Chapter 1 - 2

# HTMLでwebサイトの構造を作る

## 目標

画像のようなwebページを作成し、テキスト、リンク、リスト、画像など、基本的なHTMLの書き方を習得しましょう。

<Image src="/img/01-02-html-01.png" alt="html logo" width={512} height={350} />

## HTMLとは

HTML（HyperText Markup Language）は、webページを見出しや段落、箇条書きなどに構造化するためのマークアップ言語です。

```html
<body>
  <h1>HTMLのイメージ</h1>
  <p>このようなコードを書く</p>
</body>
```

## HTMLファイルを作ろう

<SyncTabMdx>
  <div data-label="windows">
    デスクトップに`web`フォルダを作成します。
    <div className="w-56"><Image src="/img/01-02-windows-01.png" alt="screenshot" width={512} height={384} /></div>
    VSCodeの左上のメニューから、File → Open Folderを選択します。
    <Image src="/img/01-02-windows-02.png" alt="screenshot" width={512} height={384} />
    先ほど作成した`web`フォルダを選択し、**フォルダーの選択**をクリックします。
    <Image src="/img/01-02-windows-03.png" alt="screenshot" width={512} height={384} />
    VSCodeのファイル作成ボタンをクリックし、`index.html`ファイルを作成します。
    <Image src="/img/01-02-windows-04.png" alt="screenshot" width={512} height={384} />
  </div>

  <div data-label="mac">
    Finderでデスクトップフォルダを開き、`web`フォルダを作成します。
    <Image src="/img/01-02-mac-01.png" alt="screenshot" width={512} height={354} />
    
    VSCodeを立ち上げ、Fileメニュー → Open Folderを選択します。
    <Image src="/img/01-02-mac-02.png" alt="screenshot" width={512} height={322} />
    
    先ほど作成したwebフォルダを選択し、開くをクリックします。
    <Image src="/img/01-02-mac-03.png" alt="screenshot" width={512} height={387} />
    
    VSCodeのファイル作成ボタンをクリックし、`index.html`ファイルを作成します。
    <Image src="/img/01-02-mac-04.png" alt="screenshot" width={512} height={384} />
  </div>
</SyncTabMdx>

## HTMLを書いてみよう

<SyncTabMdx>
  <div data-label="windows">
    先ほど作成した`index.html`に、以下のコードをコピペしてください。

    ```html
    <!DOCTYPE html>
    <html lang="ja">
      <!-- head : webページに表示しないコンテンツ -->
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>Title</title>
      </head>

      <!-- body : webページに表示するコンテンツ -->
      <body>
        <h1>Hello World</h1>
      </body>
    </html>
    ```

    `Ctrl` + `s` キーを押して変更を保存します。

    保存されていない変更があると、左の画像のようにファイル名の右に白丸が表示されます。右の画像のように白丸がなければ正しく保存されています。

    <div className="grid grid-cols-2 gap-4">
      <Image src="/img/01-02-html-02.png" alt="screenshot" width={512} height={358} className="m-0" />
      <Image src="/img/01-02-html-03.png" alt="screenshot" width={512} height={358} className="m-0" />
    </div>

    エクスプローラーで`web`フォルダの中の`index.html`ファイルをダブルクリックして開きます。

    以下のように、Hello Worldと表示されたら成功です。

    <div className="grid grid-cols-2 gap-4">
      <Image src="/img/01-02-windows-05.png" alt="screenshot" width={512} height={358} className="m-0" />
      <Image src="/img/01-02-windows-06.png" alt="screenshot" width={512} height={358} className="m-0" />
    </div>

  </div>

  <div data-label="mac">
    先ほど作成した`index.html`に、以下のコードをコピペしてください。

    ```html
    <!DOCTYPE html>
    <html lang="ja">
      <!-- head : webページに表示しないコンテンツ -->
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>Title</title>
      </head>

      <!-- body : webページに表示するコンテンツ -->
      <body>
        <h1>Hello World</h1>
      </body>
    </html>
    ```

    `command` + `s`キーを押して変更を保存します。

    保存されていない変更があると、左の画像のようにファイル名の右に白丸が表示されます。右の画像のように白丸がなければ正しく保存されています。

    <div className="grid grid-cols-2 gap-4">
      <Image src="/img/01-02-html-02.png" alt="screenshot" width={512} height={358} className="m-0" />
      <Image src="/img/01-02-html-03.png" alt="screenshot" width={512} height={358} className="m-0" />
    </div>

    Finderで`web`フォルダの中の`index.html`ファイルをダブルクリックして開きます。

    以下のように、Hello Worldと表示されたら成功です。

    <div className="grid grid-cols-2 gap-4">
      <Image src="/img/01-02-mac-05.png" alt="screenshot" width={512} height={358} className="m-0" />
      <Image src="/img/01-02-mac-06.png" alt="screenshot" width={512} height={358} className="m-0" />
    </div>

  </div>
</SyncTabMdx>

ブラウザとVSCodeを下のように配置して、

1. HTMLコードを編集
2. 変更を保存
3. ブラウザ再読み込み

の順で実行してみると、webページがHTMLの内容を反映しているのが確認できます。

<Image
  src="/img/01-02-html-00.gif"
  alt="screenshot"
  width={512}
  height={358}
  className="m-0"
/>

前提知識として、HTML要素は、コンテンツを開始タグと終了タグで囲む構造を持ちます。

<Image src="/img/01-02-html-04.png" alt="screenshot" width={512} height={354} />

開始タグは山括弧でタグ名を囲った構造(`<タグ名>`)、終了タグはスラッシュ+タグ名を山括弧で囲った構造(`</タグ名>`)を持ちます。

要素には属性 (attribute) を設定し、要素に関する追加情報を含ませることができます。

<Image src="/img/01-02-html-05.png" alt="screenshot" width={512} height={354} />

先ほどのコードの1行目を見てください。

{/* prettier-ignore-start */}
```html
<!DOCTYPE html>
```
{/* prettier-ignore-end */}

`<!DOCTYPE html>`は、この文書がHTML5という種類のHTMLドキュメントであることをブラウザに伝えるための宣言です。

```html
<html lang="ja"></html>
```

`html`タグは、HTMLドキュメント全体を囲むタグです。`lang`属性に`ja`が設定されており、このページが日本語で書かれていることを示しています。

```html
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Title</title>
</head>
```

`head`タグの中には、ウェブページに表示しないコンテンツを記述します。

- `<meta charset="utf-8" />` : このページの文字エンコーディングがUTF-8であることを示します。これは、日本語などの多バイト文字を正しく表示するために必要です。
- `<meta name="viewport" content="width=device-width" />` : スマホなどで表示した時に適切なサイズで表示するための設定です。
- `<title>Title</title>` : このページのタイトルを定義します。これは、ブラウザのタブやブックマークなどに表示されます。

titleタグの中身を`<title>テスト</title>`のように書き換えて、保存した後にブラウザを再読み込みするとタブの文字が**テスト**に書き換わるか確認してみてください。

<div className="grid grid-cols-2 gap-4">
  <Image
    src="/img/01-02-html-06.png"
    alt="screenshot"
    width={512}
    height={358}
    className="m-0"
  />
  <Image
    src="/img/01-02-html-07.png"
    alt="screenshot"
    width={512}
    height={358}
    className="m-0"
  />
</div>

```html
<body>
  <h1>Hello World</h1>
</body>
```

`body`タグの中には、ウェブページに表示するコンテンツを記述します。

`h1`タグは、ページの中で1番大きい見出しを表示するためのタグです。

hは**heading**の略で、見出しという意味です。

`h1`〜`h6`まであり数字が大きいほど上位の見出しです。

```html
<!-- head : webページに表示しないコンテンツ -->
```

`<!--` と `-->`で囲まれた部分は、コメントと呼ばれ、ブラウザに表示されません。

主な役割は以下の3つです。

- 開発時のメモ: コードの意図や仕様変更履歴などを残すことができます。
- デバッグ: 問題のある部分を一時的に無効化し、原因を特定するのに役立ちます。
- コードの整理: 使用していないコードや将来使用する予定のコードを一時的に非表示にすることができます。

では、他のHTMLタグも書き足してみましょう。

### pタグ

先ほどのコードの、`h1`タグの下に以下のように入力しましょう。

```html
<p>pタグ : 段落を表す要素。paragraph。</p>
<p>
  pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。
</p>
```

<details>
<summary>全体のコード</summary>
```html
<!DOCTYPE html>
<html lang="ja">
  <!-- head : webページに表示しないコンテンツ -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Title</title>
  </head>

  <!-- body : webページに表示するコンテンツ -->

  <body>
    <h1>Hello World</h1>
    <p>pタグ : 段落を表す要素。paragraph。</p>
    <p>pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。</p>
  </body>
</html>
```
</details>

pは**paragraph**の略で、テキストの段落を表示します。

ブラウザ上では通常、1行分の改行が挿入されます。上のコードも、1つ目と2つ目の`p`タグの間が改行されて表示されるはずです。

### ul, ol, liタグ

以下のように入力しましょう。

```html
<ul>
  <li>ulタグ : 箇条書きリスト。unordered list</li>
  <li>olタグ : 順序付きリスト。ordered list</li>
  <li>liタグ : リスト内の項目。list item</li>
</ul>
```

<details>
<summary>全体のコード</summary>
```html
<!DOCTYPE html>
<html lang="ja">
  <!-- head : webページに表示しないコンテンツ -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Title</title>
  </head>

  <!-- body : webページに表示するコンテンツ -->

  <body>
    <h1>Hello World</h1>
    <p>pタグ : 段落を表す要素。paragraph。</p>
    <p>pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。</p>
    <ul>
      <li>ulタグ : 箇条書きリスト。unordered list</li>
      <li>olタグ : 順序付きリスト。ordered list</li>
      <li>liタグ : リスト内の項目。list item</li>
    </ul>
  </body>
</html>
```
</details>

ulは**unordered list**の略で、箇条書きリストを表示します。

liは**list item**の略で、リスト内の項目を表示します。

コード内の`ul`タグを、`ol`タグに書き換えると、順序つきリストに変更されます。

```html
<ol>
  <li>ulタグ : 箇条書きリスト。unordered list</li>
  <li>olタグ : 順序付きリスト。ordered list</li>
  <li>liタグ : リスト内の項目。list item</li>
</ol>
```

### aタグ

以下のように入力しましょう。

```html
<a href="https://developer.mozilla.org/ja/docs/Learn/HTML/Introduction_to_HTML">
  HTML 入門 - ウェブ開発を学ぶ | MDN
</a>
```

<details>
<summary>全体のコード</summary>
```html
<!DOCTYPE html>
<html lang="ja">
  <!-- head : webページに表示しないコンテンツ -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Title</title>
  </head>

  <!-- body : webページに表示するコンテンツ -->

  <body>
    <h1>Hello World</h1>
    <p>pタグ : 段落を表す要素。paragraph。</p>
    <p>pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。</p>
    <ul>
      <li>ulタグ : 箇条書きリスト。unordered list</li>
      <li>olタグ : 順序付きリスト。ordered list</li>
      <li>liタグ : リスト内の項目。list item</li>
    </ul>
    <a href="https://developer.mozilla.org/ja/docs/Learn/HTML/Introduction_to_HTML">
      HTML 入門 - ウェブ開発を学ぶ | MDN
    </a>
  </body>
</html>
```
</details>

aは**anchor**の略で、別のウェブページ、ファイル、メールアドレス、同一ページ内の場所、または他の URL へのリンクを表示します。

`href`属性にリンク先のURLを指定します。

### imgタグ

<img src="/sample/cup.png" alt="cup" className="m-0 w-56" />
<a href="/sample/cup.png" download>
  cup.png
</a>

上のリンクをクリックし、`cup.png`をダウンロードしてください。

ダウンロードフォルダの`cup.png`を、`web`フォルダに移動します。

`index.html`に以下のように入力しましょう。

```html
<img src="./cup.png" alt="カップ" width="480" />
```

<details>
<summary>全体のコード</summary>
```html
<!DOCTYPE html>
<html lang="ja">
  <!-- head : webページに表示しないコンテンツ -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Title</title>
  </head>

  <!-- body : webページに表示するコンテンツ -->

  <body>
    <h1>Hello World</h1>
    <p>pタグ : 段落を表す要素。paragraph。</p>
    <p>pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。</p>
    <ul>
      <li>ulタグ : 箇条書きリスト。unordered list</li>
      <li>olタグ : 順序付きリスト。ordered list</li>
      <li>liタグ : リスト内の項目。list item</li>
    </ul>
    <a href="https://developer.mozilla.org/ja/docs/Learn/HTML/Introduction_to_HTML">
      HTML 入門 - ウェブ開発を学ぶ | MDN
    </a>
    <img src="./cup.png" alt="カップ" width="480" />
  </body>
</html>
```
</details>

imgは**image**の略で、webページに画像を埋め込みます。

`src`属性に、埋め込みたい画像へのパスを指定します。

`alt`属性に、画像のテキストによる説明を指定します。
`alt`属性は必須ではありませんが、画像がリンク切れした時に代わりに表示されたり、スクリーンリーダーで読み上げられたりします。

`width`属性に、画像の横幅を指定します。
`width`属性も必須ではありませんが、これを削除すると`cup.png`の元の大きさで表示されます。

### divタグ

今、ブラウザの表示が以下のように、画像の左下にリンクが来てしまっているかと思います。

もし縦に並んでいたら、ウィンドウサイズを広げると横に並びます。

<Image src="/img/01-02-html-08.png" alt="screenshot" width={512} height={358} />

HTML要素には、**ブロックレ**ベルコンテンツと、**インライン**レベルコンテンツが存在します。

ブロックレベルコンテンツは、独立したブロックとして表示され、前後に改行が入ります。

インラインレベルコンテンツは、文章中などに挿入され、前後に改行が入りません。

これまで出てきた、

- `h1`
- `p`
- `ul`
- `ol`
- `li`

はデフォルトでブロックレベルコンテンツとしてレイアウトされますが、

- `a`
- `img`

はインラインレベルコンテンツとしてレイアウトされるため、横並びに表示されています。

デフォルトでブロックレベルコンテンツである`div`タグで`a`タグを囲むことで、リンクの前後で改行されます。

```html
<div>
  <a
    href="https://developer.mozilla.org/ja/docs/Learn/HTML/Introduction_to_HTML"
  >
    HTML 入門 - ウェブ開発を学ぶ | MDN
  </a>
</div>
<img src="./cup.png" alt="カップ" width="480" />
```

<details>
<summary>全体のコード</summary>
```html
<!DOCTYPE html>
<html lang="ja">
  <!-- head : webページに表示しないコンテンツ -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Title</title>
  </head>

  <!-- body : webページに表示するコンテンツ -->

  <body>
    <h1>Hello World</h1>
    <p>pタグ : 段落を表す要素。paragraph。</p>
    <p>pタグで囲まれたテキストは、ブラウザ上では通常、1行分の改行が挿入され、段落として表示されます。</p>
    <ul>
      <li>ulタグ : 箇条書きリスト。unordered list</li>
      <li>olタグ : 順序付きリスト。ordered list</li>
      <li>liタグ : リスト内の項目。list item</li>
    </ul>
    <div>
      <a href="https://developer.mozilla.org/ja/docs/Learn/HTML/Introduction_to_HTML">
        HTML 入門 - ウェブ開発を学ぶ | MDN
      </a>
    </div>
    <img src="./cup.png" alt="カップ" width="480" />
  </body>
</html>
```
</details>

<Image src="/img/01-02-html-09.png" alt="screenshot" width={512} height={358} />

divは**division**の略で、単純に要素をグループ化するタグです。

以上で実務でよく使用するHTMLタグの使い方を習得しました。

## 参考サイト

HTMLには他にもたくさんのタグがあるので、以下のサイトで確認してみてください。

<a href="https://developer.mozilla.org/ja/docs/Web/HTML/Element">
  HTML 要素リファレンス - HTML: ハイパーテキストマークアップ言語 | MDN
</a>

また、より詳しくHTMLについて学びたい人は以下のページも参考にしてください。

<a href="https://developer.mozilla.org/ja/docs/Learn/HTML">
  HTML の学習: ガイドとチュートリアル - ウェブ開発を学ぶ | MDN
</a>
